# Semantic Advination Core

Ядро семантической адивинации команд для Jarvis-подобных систем.

## 📖 Описание

Semantic Advination Core - это модульная система для интеллектуального завершения и композиции команд, которая:

1. **Угадывает** существующие команды по префиксу (адивинация)
2. **Адаптирует** похожие команды под текущий контекст
3. **Компонует** новые команды через диалог с пользователем
4. **Откладывает** нерешаемые задачи для последующей обработки

## 🚀 Быстрый старт
### Установка

```bash
git clone <repository-url>
cd semantic_advination_core
pip install -r requirements.txt

Инициализация базы данных
python scripts/setup_database.py
Запуск демо
python main.py
Архитектура

1. Core (Ядро)
Adivinator - чистая адивинация, поиск команд по префиксу

Validator - валидация и адаптация команд под контекст

Composer - композиция новых команд через диалог

Orchestrator - управление workflow и координация компонентов

2. Storage (Хранилище)
CommandTrie - Trie-структура для быстрого поиска команд

Поддержка персистентности на диске

3. Adapters (Адаптеры)
ShellAdapter - адаптер для командной оболочки

Легко расширяется для других платформ

📊 Типы результатов
Система возвращает четыре типа результатов:

SUGGEST_EXACT - точное совпадение найдено

SUGGEST_ADAPTED - адаптированное предложение

START_DIALOG - требуется диалог для уточнения

DEFER - задача отложена для последующей обработки

🔧 Конфигурация
Настройки системы находятся в config/defaults.yaml:

Настройки системы находятся в config/defaults.yaml:
adivinator:
  min_prefix_length: 2
  partial_threshold: 0.3
  max_exact_results: 5
  max_partial_results: 3

orchestrator:
  enable_adaptation: true
  enable_composition: true
  enable_deferral: true
  
deferral:
  base_delay_hours: 24
  max_retries: 3
  exponential_backoff: true
🧪 Пример использования
from core.orchestrator import ProductionOrchestrator
from storage.trie_storage import CommandTrie

# Инициализация
storage = CommandTrie("data/commands.db")
orchestrator = ProductionOrchestrator(storage)

# Обработка запроса
result = orchestrator.process_request("git commit -m", {"domain": "git"})

if result.outcome == "SUGGEST_EXACT":
    print(f"Найдено: {result.suggestions[0].text}")
elif result.outcome == "START_DIALOG":
    print(f"Вопрос: {result.first_question}")
    # Обработка ответа пользователя
    next_result = orchestrator.continue_dialog(result.dialog_id, "fix typo")
    
Тестирование
pytest tests/ -v
semantic_advination_core/
├── core/           # Основное ядро системы
├── storage/        # Хранилище команд
├── adapters/       # Адаптеры для платформ
├── config/         # Конфигурация
├── data/           # Данные и базы данных
├── tests/          # Тесты
├── scripts/        # Вспомогательные скрипты
└── docs/           # Документация
MIT License
